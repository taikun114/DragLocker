# AGENTS

## プロジェクトについて

プロジェクト名: **DragLocker**

ターゲットプラットフォームは次の通り。

- macOS 14.0またはそれ以降

使用されているパッケージは次の通り。

- **KeyboardShortcuts**
  - ユーザー独自のショートカットキーを設定・管理するためのライブラリ。ドラッグロック監視の有効・無効の切り替えショートカットに使用している。

### プロジェクト概要

**DragLocker**は、macOS上でマウスのドラッグ状態をロック（保持）するためのユーティリティアプリである。マウスボタンを一定時間押し続ける、または一定距離ドラッグすることでドラッグ状態を自動的にロックし、指への負担を軽減することを目的としている。ロックに対応するマウスボタンは左マウスボタン（ボタン0）、右マウスボタン（ボタン1）、スクロールホイールボタン（ボタン2）である。
SwiftUIで構築されており、メニューバー常駐型のエクスペリエンスを提供する。

## 開発方針と注意事項について

### 開発方針

#### ローカライズに関して

- 「ドラッグロック」を英語で記載する場合は、現在進行形（Drag Locking）やハイフン付き（Drag-Lock）を避け、**Drag Lock** に統一する。
- **`Localizable.xcstrings` の更新について**:
  - コード内に追加した新しい翻訳キーを `Localizable.xcstrings` に反映（再インデックス）させるには、XcodeのMCPツール（`BuildProject`）またはXcodeのGUIでビルドを行う必要がある。
  - シェルコマンド（`xcodebuild`など）でビルドしただけでは新しいキーがファイルに認識されないため注意すること。MCPツールが利用できない環境では、ユーザーにGUIでのビルドを依頼するか、手動でキーを追加して対応すること。

#### 表記について

- 日本語では、「〜するとき」などの形式名詞としての「とき」はひらがなで統一する（「〇〇時」などの特定の動作や時刻を表す場合は漢字を使用する）。

#### デバッグログについて

- ログ出力には `print` を使用する。
- 詳細な挙動を確認するためのログは `#if DEBUG` で囲み、リリースビルドには含まれないようにする。
- ただし、リリース後にユーザーから報告された不具合の原因を特定しやすくするため、重要な状態の変化やエラーの発生に関するログは `#if DEBUG` で囲まず、リリースビルドでも出力されるように一部残すこと。

#### パフォーマンスと応答性について

- アプリの動作を軽量に保つため、可能な限り処理をバックグラウンドスレッドで実行し、UIのフリーズを回避（または最小限に抑制）するように実装すること。

#### 新機能の実装について

- 新機能を実装する際やリファクタリングを行う際、今後の開発持続性やメンテナンス性を高めるため、可能な限り再利用可能なコンポーネントを作成して使用すること
- あらゆるユーザーが簡単にDragLockerを使用できるよう、様々なアクセシビリティに配慮した設計・実装を行うこと
  - 例えば、レイアウトの関係により、ラベルを非表示に（`.labelsHidden()`などを付与）したスイッチやピッカーでもVoiceOverで読み上げられるように、ラベルテキストを設定すること
